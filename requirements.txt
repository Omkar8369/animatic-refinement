# Aggregator for all pipeline Python dependencies.
#
# Each node folder that adds runtime deps owns its own requirements file;
# this file -r-includes them. When a new node ships with Python deps,
# add a corresponding -r line below.
#
#   runpod_setup.sh runs `pip install -r requirements.txt` on the pod,
#   which transitively installs every per-node file.
#
# Frontend (Node 1) runs in the browser; no Python deps.
# ComfyUI's torch/torchvision/safetensors belong to ComfyUI itself, not here.

# Nodes 2 + 3 + 4 + 5 (+ Node 11 later) - pure-Python pipeline core
# (pydantic + imageio-ffmpeg + numpy + pillow + scipy)
-r pipeline/requirements.txt
