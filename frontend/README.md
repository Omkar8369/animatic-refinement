# Node 1 - Project Input & Setup Interface

HTML form that captures per-shot metadata plus asset uploads (MP4 shots, character model sheets) and writes `metadata.json` as the hand-off into `pipeline/`.

See [../docs/PLAN.md](../docs/PLAN.md) Node 1 for full sub-step breakdown.

**Status:** Pending
